# Aphenas Mobile App

Local setup uses `EXPO_PUBLIC_API_BASE_URL` and `EXPO_PUBLIC_SOCKET_URL`.

For Expo Go through ngrok, point both values to:

```text
https://duffel-treble-cube.ngrok-free.dev
```
